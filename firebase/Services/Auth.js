/* import { auth, provider } from '../firebase'

export const SingInWithGoogle = () => {
    let user;
    try {
        auth.signInWithEmailAndPassword('manliotejeda@gmail.com', '123698745')
            .then(res => {
                console.log(res)
            })
            .catch(error => {
                console.log(error)
            })
    } catch (e) {
        console.log(e)
    }
    return user
} */
